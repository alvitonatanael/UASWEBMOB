import 'package:flutter/material.dart';
import 'login_page.dart'; // Pastikan Anda membuat file `login_page.dart` untuk kode login

void main() => runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      home: LoginPage(),
    ));
